import { Shield, LogIn } from "lucide-react";

export default function LoginPage({ onLogin }) {
  return (
    <div className="min-h-screen bg-slate-50 flex items-center justify-center px-6">
      <div className="w-full max-w-md bg-white rounded-2xl border border-slate-200 shadow-sm px-8 py-10">
        <div className="flex items-center gap-3 mb-8">
          <img
            src="/HSBC_MASTERBRAND_LOGO_RGB.png"
            alt="HSBC"
            style={{ height: 40 }}
          />
          <div className="h-7 w-px bg-slate-200" />
          <span className="text-sm font-semibold text-slate-600 tracking-wide">
            DOT Intelligence Platform
          </span>
        </div>

        <div className="h-10 w-10 rounded-xl bg-blue-50 flex items-center justify-center mb-4">
          <Shield size={20} className="text-blue-700" />
        </div>

        <h1 className="text-xl font-semibold text-slate-800">Sign in</h1>
        <p className="text-sm text-slate-500 mt-2 leading-relaxed">
          Use your HSBC account. Access to feeds, reconciliation and action
          taken reports depends on the role assigned to you.
        </p>

        <button
          onClick={onLogin}
          className="mt-7 w-full flex items-center justify-center gap-2 px-4 py-3 rounded-xl
            text-sm font-semibold text-white transition-all hover:opacity-90"
          style={{ background: "linear-gradient(135deg, #1d4ed8, #1e3a8a)" }}
        >
          <LogIn size={16} /> Continue with Microsoft
        </button>

        <p className="text-xs text-slate-400 mt-5 leading-relaxed">
          Signing in does not grant access on its own. If your account has not
          been added to the platform yet, you will be told who to contact.
        </p>
      </div>
    </div>
  );
}